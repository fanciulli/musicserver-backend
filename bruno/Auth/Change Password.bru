meta {
  name: Change Password
  type: http
  seq: 3
}

post {
  url: http://{{host}}:{{port}}/admin/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "password",
    "newPassword": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
